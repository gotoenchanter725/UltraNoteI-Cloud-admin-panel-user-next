/*
 * PhoneVarification Messages
 *
 * This contains all the text for the PhoneVarification container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PhoneVarification';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PhoneVarification container!',
  },
});
