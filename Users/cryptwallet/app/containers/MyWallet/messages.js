/*
 * MyWallet Messages
 *
 * This contains all the text for the MyWallet container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MyWallet';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MyWallet container!',
  },
});
