/*
 * BreadCrumbs Messages
 *
 * This contains all the text for the BreadCrumbs container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BreadCrumbs';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BreadCrumbs container!',
  },
});
