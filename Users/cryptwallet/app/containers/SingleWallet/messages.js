/*
 * SingleWallet Messages
 *
 * This contains all the text for the SingleWallet container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SingleWallet';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SingleWallet container!',
  },
});
