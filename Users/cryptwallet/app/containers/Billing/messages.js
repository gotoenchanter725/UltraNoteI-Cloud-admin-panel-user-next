/*
 * Billing Messages
 *
 * This contains all the text for the Billing container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Billing';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Billing container!',
  },
});
