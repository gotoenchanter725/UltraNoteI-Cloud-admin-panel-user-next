/*
 * SingleWalletActivity Messages
 *
 * This contains all the text for the SingleWalletActivity container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SingleWalletActivity';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SingleWalletActivity container!',
  },
});
