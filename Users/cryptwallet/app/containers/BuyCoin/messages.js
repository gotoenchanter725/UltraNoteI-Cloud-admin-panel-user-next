/*
 * BuyCoin Messages
 *
 * This contains all the text for the BuyCoin container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BuyCoin';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BuyCoin container!',
  },
});
