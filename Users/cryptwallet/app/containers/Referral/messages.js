/*
 * Referral Messages
 *
 * This contains all the text for the Referral container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Referral';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Referral container!',
  },
});
