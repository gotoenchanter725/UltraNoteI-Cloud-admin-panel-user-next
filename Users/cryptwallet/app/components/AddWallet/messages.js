/*
 * AddWallet Messages
 *
 * This contains all the text for the AddWallet component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AddWallet';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddWallet component!',
  },
});
