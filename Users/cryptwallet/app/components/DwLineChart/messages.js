/*
 * DwLineChart Messages
 *
 * This contains all the text for the DwLineChart component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DwLineChart';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DwLineChart component!',
  },
});
