/*
 * WalletActivityTable Messages
 *
 * This contains all the text for the WalletActivityTable component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.WalletActivityTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WalletActivityTable component!',
  },
});
