/*
 * SubmitIdCard Messages
 *
 * This contains all the text for the SubmitIdCard component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SubmitIdCard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SubmitIdCard component!',
  },
});
