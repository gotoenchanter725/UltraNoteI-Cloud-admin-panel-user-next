/*
 * MessageDetail Messages
 *
 * This contains all the text for the MessageDetail component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MessageDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MessageDetail component!',
  },
});
